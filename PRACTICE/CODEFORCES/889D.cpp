// हर हर महादेव
#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll n;
    cin >> n;
    vector<ll> v(n);
    for (int i = 0; i < n; i++)
        cin >> v[i];
    // ll sum = 0;
    // ll pre = 1;
    // ll ans = 0;
    // int i = 0;
    // while (i < n && pre > sum)
    // {
    //     if (sum + 1 == pre && i < n - 1)
    //     {
    //         pre += v[i];
    //     }
    //     else
    //     {
    //         ans += v[i];
    //     }
    //     i++;
    //     sum++;
    // }
    // cout << ans << endl;

    return 0;
}
